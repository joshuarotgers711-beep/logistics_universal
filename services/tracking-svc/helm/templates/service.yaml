apiVersion: v1
kind: Service
metadata:
  name: tracking-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: tracking-svc
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8085

