apiVersion: v1
kind: Service
metadata:
  name: label-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: label-svc
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8084

