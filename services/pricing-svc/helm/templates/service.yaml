apiVersion: v1
kind: Service
metadata:
  name: pricing-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: pricing-svc
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8083

