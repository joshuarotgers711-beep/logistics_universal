apiVersion: v1
kind: Service
metadata:
  name: shipment-svc
spec:
  type: {{ .Values.service.type }}
  selector:
    app: shipment-svc
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8082

