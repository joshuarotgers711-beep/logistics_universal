apiVersion: apps/v1
kind: Deployment
metadata:
  name: shipment-svc
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: { app: shipment-svc }
  template:
    metadata:
      labels: { app: shipment-svc }
    spec:
      containers:
        - name: shipment-svc
          image: {{ .Values.image.repository }}:{{ .Values.image.tag }}
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8082
          env:
            - name: PORT
              value: {{ .Values.env.PORT | quote }}
          readinessProbe:
            httpGet: { path: /ready, port: 8082 }
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet: { path: /health, port: 8082 }
            initialDelaySeconds: 3
            periodSeconds: 10

