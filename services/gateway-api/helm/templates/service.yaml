apiVersion: v1
kind: Service
metadata:
  name: gateway-api
spec:
  type: {{ .Values.service.type }}
  selector:
    app: gateway-api
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 8080

